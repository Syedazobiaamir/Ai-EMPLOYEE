# AI Employee Watcher Dependencies (Silver Tier)
# Install with: pip install -r watchers/requirements.txt

# ── Bronze Tier ──────────────────────────────────────────────────────────────
# File System Watcher
watchdog>=4.0.0

# ── Silver Tier ──────────────────────────────────────────────────────────────

# Gmail Watcher
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0
google-api-python-client>=2.0.0

# WhatsApp Watcher (Playwright browser automation)
playwright>=1.40.0
# After install, run: playwright install chromium

# LinkedIn Watcher (HTTP requests)
requests>=2.28.0

# Shared utilities
python-dotenv>=1.0.0
